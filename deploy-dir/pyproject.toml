[project]
name = "lb-workspace"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "lexical-benchmark",
]

[tool.uv.sources]
lexical-benchmark = { path = "code", editable = true }
